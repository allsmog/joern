Four macro Binding source controls: three new projects plus the exact repeated_direct_include anchor, seven source files. The fresh canonical run and separate saved-graph observer are complete and admitted. Every expected.txt is the complete LF-only canonical projection extracted from admitted raw output; every supplement.jsonl retains the complete typed Joern node/property/edge stream. None was edited or filtered.

Accepted Rust at a806866 matches all four complete canonical outputs: 712 nonempty records, zero differences. This projection omits Binding nodes. Complete snapshots show Joern214 nodes/756 edges versus accepted Rust210/578, including four missing macro Binding components; other full property/edge diagnostics remain. The ADD and shared VALUE controls each have two invocations but one macro Binding. The unused local macro has no METHOD or Binding. The shared-header Binding's owner is a.c:<global>, although its METHOD_FULL_NAME starts shared.h. These are observed controls, not a universal first-file ownership rule.

All nine observed Bindings, including five ordinary bindings and four macro bindings, are retained. [derive-metadata.py](derive-metadata.py) derives each expected-binding-metadata.json directly from the complete snapshot. It retains every Binding property and incident edge occurrence, verifies null edge payloads, and includes the physical owner, target method and incoming CALL multiplicity. Run the script with this family directory and --check to verify the derived files without rewriting them.

[macro_bindings.rs](../../macro_bindings.rs) adds two production tests: all four complete canonical graphs, and all nine Binding components before and after save/load. The property assertion includes every represented optional string column, including METHOD_FULL_NAME, plus line and column. New macro bindings must have explicit absent ORDER; accepted ordinary Binding ORDER stays Unknown. Dense order 0 and argument index -1 are checked separately as storage invariants, not inferred Joern properties. The current edge storage has no payload column, and ARGUMENT_INDEX presence remains unsupported. Complete raw snapshots retain those boundaries and all other full-graph diagnostics.

The candidate source appends finalized macro bindings after existing ordinary bindings and include/dependency nodes, using the already resolved macro owner. No schema or CPG2v3 change is involved; cache shape becomes 25. Source and tests are prepared, but compilation and candidate graph retention have not run at this freeze. Future build/replay evidence must be retained separately; this package makes no candidate acceptance claim.

provenance contains complete canonical/observer raw stdout/stderr, admitted run receipts, accepted Rust raw outputs/statuses and saved CPG2v3 files, prepared scripts/oracle, runtime inventories and accepted checkpoint bindings. Actual Joern/JDK and executable trees are not copied. Absolute paths in historical receipts identify their original immutable artifacts; copy-provenance.json maps every directly copied file to its original path, hash and portable package path. The preserved template remains unapproved history; actual reference runs used separately issued parent releases. No new producer ran during this packaging step.

The initial staging README is preserved in [provenance/historical-stage-README.md](provenance/historical-stage-README.md), alongside the two original stage manifests. The four cases/*/accepted/accepted-v3.cpg files are actual accepted graph artifacts and require force-staging under the repository's ignore rules.
