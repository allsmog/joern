# Tenth measured C parity batch — 2026-09-08

This checkpoint improves C parity over the [accepted ninth source](astra-ninth-batch-2026-09-08.md) and passes every final gate. The port remains incomplete; neither complete zlib nor Lua projection is exact. The final integrated build, all 138 Rust/Cargo input hashes, review receipts and remaining limits are recorded in the [acceptance record](astra-tenth-batch-acceptance.json). The [complete counters](astra-tenth-batch-metrics.json) retain all projected differences.

Active supplied headers inside function bodies now contribute declarations through a distinct view for each include occurrence. Statement emission, declaration discovery, type sites and prototypes share that view and its source-position macro context. Included declarations retain the caller's lexical block; local typedefs retain the physical header identity. The importer locates included initializers against header tokens, preserving the following caller expression's line. This is measured line support, with raw column/end-position quirks retained. [Body-include implementation, source origins and complete controls](../../cpg-rs/joern-parity/tests/fixtures/body-includes/README.md) describe the boundary.

The unchanged 14 body-include references and four separate array-dimension references form **18 unique projects: 6 exact on accepted ninth, 11 on held V2, and 14 on V4**. All 14 exact selected graphs have complete production comparisons; four diagnostics remain. The unchanged anchor is retained in the new raw oracle batch without adding a duplicate gate. The union contains 7,125 canonical LF lines, including separators, and 6,991 nonempty selected records. Multiplicity-preserving comparison gains **1,284 nonempty matching records and loses two**; including canonical separators gives **1,288 gains and the same two losses**. [Array-family measurement](../../cpg-rs/joern-parity/tests/fixtures/array-dimension-macros/measurement.json) binds these counts and every complete input/reference/output.

The two raw losses remain counted. Accepted `second#12/#13` identify a return expression's receiver and index; the same oracle ordinals identify a newly included initializer and literal. The original index occurrence survives, and the receiver gains the declared array type. [Complete endpoint and ancestor evidence](../../cpg-rs/joern-parity/tests/fixtures/body-includes/provenance/ordinal-collisions.json) demonstrates the ordinal coincidences. Separately, all **76 prior body-macro-state projects** were retained and replayed: every successful graph stdout is byte-identical to V2, preserving the earlier 54→55 exact result and its 25 matching-record gains. The `duplicate_clinit_tag` failure remains explicit: accepted ninth and V4 return −6, V2 debug returns 101, and all three have empty stdout with the same assertion failure. [Prior-family retention and actual statuses](../../cpg-rs/joern-parity/tests/fixtures/array-dimension-macros/provenance/candidate-v4/prior76.json) keep those diagnostics separate from the 18-project union.

V2 was held for a real regression: expanding every array dimension changed Joern's retained spelling, for example `MAX_BITS+1` to `15+1`. Fresh paired controls establish the narrower rule: expand bare `N`, but preserve `N+1`, `(N+1)` and `+N`. V4 applies that guard and makes all four new graphs exact; numeric controls remain exact. It restores all **96 previously correct whole-zlib matching records** lost by V2 while retaining the bare-macro and Lua gains. The original V2 sources, outputs, failures and full differences remain intact. [The array boundary and repair](../../cpg-rs/joern-parity/tests/fixtures/array-dimension-macros/README.md) make no general constant-folding claim.

The whole review uses the unchanged 26-file zlib and 61-file Lua inputs. It explicitly reuses the sixth batch's pinned Joern 4.0.555 whole-project references; it does not claim a new whole-project Joern run. Against accepted ninth, V4 gains **8,799 matching records in zlib and 260 in Lua, with zero matching losses**. All V2 gains survive. These counts preserve multiplicity across AST, node, edge and flow sections. Selected text can identify different physical occurrences alike, so source/ancestor checks remain separate from raw counting. [Independent complete-output and source-fact review](../../.local/astra-sprint/tenth-batch/worker-v4-whole-review.json) binds both comparisons and all remaining differences.

| Exact projected AST measure | Zlib, ninth → V4 | Lua, ninth → V4 |
|---|---:|---:|
| METHOD ASTs, including stubs |160 → 160 of 410|1,434 → 1,438 of 2,274|
| Primary source METHOD ASTs |42 → 42|513 → 517|
| Primary function bodies |42 → 42|540 → 544|
| Nonempty primary bodies |23 → 23|518 → 522|

The four new exact Lua methods are `luaG_addinfo`, `luaO_tostring`, `os_tmpname` and `utf8esc`. No previously exact method is lost. All 61 tracked repair facts, 19 selected method trees/stubs and three actual zlib `snprintf` calls retain their source-occurrence evidence. The [same whole review](../../.local/astra-sprint/tenth-batch/worker-v4-whole-review.json) supplies the table's strict primary/body identities and complete priority methods.

Both zlib `fixedtables` methods now contain all 544 initializer rows and 1,632 literal values per method from `inffixed.h`, with AST topology matching Joern. All prior matched properties, structural edges and reaching definitions survive. Their remaining method identities, six long CODE fields per method and associated flow-label differences prevent full-method parity. [The occurrence review](../../.local/astra-sprint/tenth-batch/fixedtables-whole-review.json) retains these gaps; the V4 review verifies both full trees are unchanged from V2.

Resource acceptance was also held. V2's two zlib builds took **17.29/17.31 seconds and 538.625/542.40625 MiB**, exceeding the unchanged 512 MiB ceiling. The [failed run and complete log](../../.local/astra-sprint/tenth-batch/final/resource-measurement-run.json) remain; the driver stopped before update and Lua measurements. V3 replaces repeated allocation/sorting of pass-through mappings with borrowed membership checks. Its [single diagnostic pilot](../../cpg-rs/joern-parity/tests/fixtures/array-dimension-macros/provenance/performance-v3/pilot/run.json) measured **3.629 seconds and 488.65625 MiB**, with graph and edge bytes identical to V2. That pilot is not repeated acceptance, and V4's dimension repair changes graph properties afterward.

The four new-family diagnostics remain explicit: repeated-include macro fallback/standalone-header phantom differences; seven primitive member-type spelling differences shared by the tiny included and inline fixedtables controls; and the quoted-header resolver mismatch. Full zlib static identities and long CODE, broader preprocessing, expression/type lowering, transport, schema, query and non-C coverage remain outside this increment's claims. [Fixture boundaries](../../cpg-rs/joern-parity/tests/fixtures/body-includes/README.md) and [compatibility scope](../../cpg-rs/COMPATIBILITY.md) preserve these limits.

The final integrated release passes **468 workspace tests in 76 result groups**, the unchanged **308/308** committed and fresh live Joern comparisons, 17 checker tests, formatting, strict Clippy and the 73-dependency audit. Native macOS ARM64 release/archive and official real-project acceptance pass. All 138 frozen Rust/Cargo inputs and both release binaries remain unchanged through validation. Cache shape is 21. No container or other-platform execution is claimed.

Two final zlib builds take **3.18/3.28 seconds**, using **489.31/498.78 MiB**; Lua takes **7.18/7.00 seconds**, using **455.44/465.77 MiB**. They satisfy the unchanged 20-second and 512/1,024 MiB build ceilings. Repetitions produce identical graph, edge, export and zero-finding SARIF bytes. Clean/update equivalence passes for 26/61 files, taking 9.55/21.40 seconds with separate RSS 640.69/720.91 MiB; update measurements are not compared with build ceilings. Known task producers had finished before these measurements; unrelated host activity was not controlled.

The [independent root review](../../.local/astra-sprint/tenth-batch/repaired-final/manifest-whole-resource-review.json) verifies final whole output equality with the reviewed worker, resource artifacts, pinned inputs/licenses and exactly **eight expected-output changes**: node counts and graph/edge/export hashes for both projects. Source/archive/license/exclusion/budget/SARIF contracts stay fixed. Official acceptance subsequently passes using that applied manifest; its receipt and log are bound in the acceptance record. The original checkout and all 13 existing dirty files remain unchanged. This checkpoint is local on `codex/astra-parity-sprint`; no push or merge is claimed.

The fixture packages preserve their worker-stage READMEs and raw evidence unchanged; their ROOT_ACCEPTANCE notes link to this final status. Large whole-project outputs and executables remain local at bound paths rather than being shipped inside the small fixture corpus. The next separate unit has 17 new primitive-member projects and two unchanged tiny-fixedtables anchors prepared. It has not run a fresh oracle or changed implementation yet.
