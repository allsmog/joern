#define N 3
int macro_dimension(int index) {
    int values[+N];
    values[0] = 7;
    return values[index];
}
int literal_dimension(int index) {
    int values[+3];
    values[0] = 7;
    return values[index];
}
