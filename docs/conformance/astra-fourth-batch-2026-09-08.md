Fourth measured C parity batch — 2026-09-08

This batch improves the Rust C frontend’s treatment of arrays, supplied-header macros, statement macros, and reaching definitions against Joern v4.0.555. It also reduces graph construction and export memory and fixes format-string sink positions. The final implementation passes the workspace and differential checks, repeated real-project measurements, and native packaging checks. **Source checkpoint: `862e54ee5a4cd6efaff156bb0f844090ebce5e89`. Official acceptance: passed.** This is an increment toward the port; neither whole-project graph is exact. All counters and evidence bindings are in the [metrics](astra-fourth-batch-metrics.json) and [acceptance receipt](astra-fourth-batch-acceptance.json).

Work began on 2026-09-08 at 09:58 UTC, from accepted checkpoint `06c3a5524628e6d9da8e311f272fb9ffd0856fba`, whose production source baseline is `b8c2ae132`. Implementation and review use isolated local worktrees; the original checkout is preserved. No changes have been pushed or merged. This report records a bounded C parity batch, not completion of the Joern port.

The oracle remains Joern v4.0.555 under JDK 21.0.12. Fresh fourth-batch whole-project oracle runs reproduce the third-batch canonical reference bytes. The inputs remain the same unmodified upstream source selections: 26 zlib 1.3.1 files, containing 22,732 lines, and 61 Lua 5.4.7 files, containing 30,098 lines. No source patches or extra compiler defines were introduced to make these comparisons easier. Binary, source, input-manifest, and raw-output hashes are recorded in the [final metrics](astra-fourth-batch-metrics.json) and [frozen source bindings](/Users/shayaunnejad/vibe-code/.codex-worktrees/joern-oxidized-astra-sprint/.local/astra-sprint/fourth-batch/final-real-differential-v2/bin/source-bindings.json). These identify the measured working changes; the parent revision alone does not identify an uncommitted final.

Array lowering now covers the pinned declaration grouping, allocation and initializer forms, pointer/array spelling, and supported designators. Its separate suite retains 69 complete reference graphs. The source-location repair preserves declaration and initializer spans even when their graph views overlap, and checks subsequent calls and sibling declarations. Parameter spelling remains separately governed: a spelling observed for an array object is not applied indiscriminately to a parameter. The integrated reaching-definition repairs close the array suite’s earlier eight graph differences; the historical failing receipts remain available rather than being rewritten as successes.

Supplied quoted headers now contribute macro state in caller context, including ordered definitions, undefinitions, reinclusion, include guards, once behavior, and relative-path resolution within the supplied file set. Logical directive recovery handles commented multiline definitions without inventing macros from formal-parameter names. Expression expansion, copied macro arguments, wrapper types, and recursion limits have separate live-derived references. These improvements do not establish general function-like macro evaluation inside `#if` or complete preprocessing compatibility.

Statement macros now retain compound blocks, declarations, branches, loops, and return statements that were previously missing from expansion bodies. The implementation preserves lexical symbol scope and records source-derived control kinds where the selected canonical text alone is insufficient. Macro descendants receive invocation-based source locations. Tests retain complete graphs for the supported cases and complete nonconformant diagnostics for unresolved boundaries. They do not substitute a filtered set of convenient graph facts for the reference.

The reaching-definition work repairs CFG composition, solver scheduling, incoming-definition snapshots, treatment of unreachable generated definitions, and exit ordering. Focused oracle cases establish the resulting graph facts. A subsequent bitset representation change preserves complete canonical output, serialized graph bytes, and edge bytes for both whole projects. Its independent focused equivalence review also covers 154 cases and 72,953 canonical lines including separators. This is evidence for the tested transformation; it is not proof that every remaining analysis behavior matches Joern.

Whole-project results show real function-body gains alongside substantial growth in macro and operator scaffolding. The two categories must remain distinct:

| Complete selected AST comparison | zlib baseline → final | Lua baseline → final |
| --- | ---: | ---: |
| All exact method ASTs, including stubs | 93 → 127 | 516 → 1,108 |
| Exact primary source-method ASTs | 11 → 14 | 104 → 197 |
| Exact primary body BLOCK subtrees | 21 → 25 | 114 → 226 |
| Exact nonempty primary body BLOCK subtrees | 4 → 8 | 93 → 204 |

The oracle contains 410 total method ASTs for zlib and 2,274 for Lua. Primary comparisons require matching method identity and source CODE, and exclude macro stubs and global scaffolding. Full-method equality includes the selected header and parameter properties; body equality compares the complete direct BLOCK subtree. No formerly exact primary method AST is lost. The [independent semantic receipt](/Users/shayaunnejad/vibe-code/.codex-worktrees/joern-oxidized-astra-format-rules/.local/final-semantic-review/review.json) contains the classification rules, names, complete counters, and source bindings. The latest measured outputs are byte-identical to those reviewed outputs.

Three formerly exact Lua stubs are lost: `liolib.c:L_MAXLENNUM:int(0)`, `lua_tointeger`, and `lua_tonumber`. This is not a zero-regression AST result. The old `L_MAXLENNUM` stub arose from an incorrect nested invocation in `nextc`; Joern’s corresponding stub originates in an array dimension whose initializer remains missing. The two conversion stubs were previously created by ordinary header macro uses incorrectly treated as static calls. Those uses now expand, while Joern still retains static calls in its recovered `g_write` fragment. The remaining disagreement concerns that recovery context. Restoring the old incorrect invocation sites solely to recover exact stub counts would conceal the underlying differences.

All four previously identified genuine Lua reaching-definition losses are restored: the reviewed edges in `checkclosemth`, `checktoclose`, `freereg`, and `singlevar` now match the oracle’s endpoint records and immediate parent context. This check establishes the meaning of those four edges independently of their serialized addresses. Raw flow-record intersections also rise, from 3,624 to 10,833 for zlib and from 29,776 to 79,022 for Lua. Raw gains and losses remain fully recorded, but address shifts can multiply differences or create accidental matches; those counts are neither semantic bug counts nor a completion percentage.

Restored statement bodies still contain concrete gaps. `_tr_align` grows from 40 to 256 AST records against 259 in Joern, but several typedef casts become pointer calls and field macros remain unexpanded. `luaD_seterrorobj` grows from 72 to 204 records against 213; two nested cast expansions still leave an address-of node without its operand subtree. `luaG_concaterror` grows from 23 to 35 records against 39, with residual nested `rawtt` expansion and return-type differences. Every full comparison remains available. Neither whole-project projection is exact.

Scanner regressions are checked separately. The array fixture retains five live outcomes: two positives and three negatives. Statement-macro outcomes improve from three of seven to seven of seven, restoring four positives; the prior 14 standalone-block outcomes remain covered. The C format-string rule now identifies argument positions consistently with the existing C++ rule. Its 11-function regression has four positives and seven negatives. Three newly exposed Lua preview findings were false positives caused by treating every argument as a format-string sink; the repaired rule removes them. They are not reported as vulnerabilities.

JSON export now serializes borrowed graph views instead of materializing a second full JSON representation. Independent comparisons preserve all eight complete saved-graph exports byte for byte, including field order, escaping, and null behavior. Per-process peak RSS falls from roughly 823 to 48 MiB for zlib and 1,333 to 70 MiB for Lua on those frozen graphs. Review found and closed a final-buffer error regression: production dispatch now flushes before reporting success. These export measurements are separate from frontend build memory.

The latest logged validation passes all 412 workspace tests with no failures, ignored tests, or filtered tests; all 17 checker tests; formatting; strict Clippy; and the dependency audit over 73 crates. The main corpus remains exact at 308/308 comparison blocks against both committed references and a fresh live run. Native release acceptance and the macOS ARM64 archive smoke test pass. These results cover the measured final and host; no container or other-platform execution is claimed. [Validation logs](/Users/shayaunnejad/vibe-code/.codex-worktrees/joern-oxidized-astra-sprint/.local/astra-sprint/fourth-batch/final-v2/) retain the individual results.

Graph-construction memory also required repair. One earlier final attempt measured 516.72 MiB on zlib and failed its 512 MiB gate; that failed log remains intact. Reaching-definition bitsets, consuming canonical serialization buffers after use, and borrowing importer edge addresses remove redundant allocations without changing complete graph or canonical bytes. An independent importer replay checks 154 persisted graphs after dropping the original input buffers. Three isolated post-repair runs measured 483–485 MiB for zlib and 450–453 MiB for Lua.

The final frozen executable’s two acceptance measurement runs produce the following results. Other work ran concurrently, so these are host observations rather than a speed benchmark. Peak RSS is reported separately for build, export, and update; the update-equivalence process is outside the existing build RSS ceiling.

| Final measured operation | zlib 1.3.1 | Lua 5.4.7 |
| --- | ---: | ---: |
| Build time, two runs | 6.72–8.75 s | 4.02–4.09 s |
| Build peak RSS, two runs | 378.72–484.00 MiB | 451.25–460.58 MiB |
| Existing build ceiling | 20 s / 512 MiB | 20 s / 1,024 MiB |
| JSON export peak RSS | 53.50–54.17 MiB | 74.13–74.55 MiB |
| Update-equivalence time / RSS | 34.77 s / 629.63 MiB | 13.02 s / 601.17 MiB |
| Saved nodes / scan findings | 65,902 / 0 | 114,922 / 0 |

Repeated graph, edge, export, and SARIF files are byte-identical. The acceptance manifest changes only expected node counts and graph/edge/export hashes following independent semantic review. Source selection, archive hashes, licenses, exclusions, scan expectations, and resource budgets stay fixed. The separate official gate passes both projects. Its printed peak is cumulative across prior child processes, so the Lua peak also includes the earlier zlib update; it is not a standalone Lua build measurement. The full gate output and per-process observations are retained in the acceptance receipt.

All exactness claims concern the existing selected AST, node, edge, and reaching-definition projection. Its flow-label transport escapes physical newlines and is not injective or lossless; distinct source strings can serialize identically. Column numbers and some control metadata are outside the selected schema/projection. Further macro, typedef, member/designator, `offsetof`, CRLF, and preprocessing-context differences remain. Variadics, stringification, token pasting, block-level directives, and unbraced multi-statement replacements are not complete. Declaration-specifier macro lookup still uses standalone header state in some paths, so caller-dependent type spelling is not covered by the improved body-macro context. Header lookup covers supplied quoted relative paths; angle includes, external include paths, and build definitions are outside that implementation. Expansion limits and unsuccessful parsing can leave expressions unexpanded or omit unsupported syntax, so passing the retained fixtures does not establish general macro completeness. Joern’s binary format, Scala query DSL, arbitrary extensions, broader query behavior, and non-C frontend parity are not delivered by this batch. The retained evidence supports concrete improvements and bounded regressions, without a port-completion percentage or delivery-time forecast.

The integration and acceptance window spans 09:58 UTC to 13:03 UTC on 2026-09-08. It includes parallel agent work, reviews, measurements, and an interruption before the final measurements resumed; it is neither person-hours nor a model speed benchmark. The source and reference fixtures are committed locally. This report, counters, and acceptance receipt record the checkpoint for review. Large full-project outputs and detailed review artifacts remain at the hash-bound ignored local paths recorded in the receipt; those raw artifacts are not included in a fresh clone. The original checkout and its 13 dirty audit/planning files remain unchanged. The next C increment should pin the nested typedef-cast and field-macro differences illustrated above, preserving the complete differential comparisons.
