# Combined field/cast references

Three complete graphs assert cast-bearing field indexes, sibling accesses and a typed outer function macro. Three full diagnostics retain an existing missing type/member REF, expanded sizeof spacing, and Unicode function-prototype classification. Independent review reproduced those residuals in individual feature baselines; the combined change introduces no confirmed regression in these six probes. Raw Joern output, complete references and full candidate diffs are preserved. This is the existing selected canonical projection.
