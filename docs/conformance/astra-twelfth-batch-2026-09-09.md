# Twelfth measured C parity batch — 2026-09-09 UTC

C function definitions now follow Joern's duplicate naming and call-link rules. The production graph also retains method bindings, modifier properties and the physical source lines of implicit function references. Cache shape is **23**. This is a verified increment toward a complete port; **full Joern parity remains incomplete**. The [acceptance record](astra-twelfth-batch-acceptance.json) binds the tested source, binaries and checks.

Eight new complete canonical projections improve **1→8 exact**, gaining **577 matching nonempty records with no losses**. Adding three retained controls gives **4→11 exact**. The eight references came from a fresh pinned Joern 4.0.555 run. A separate observer reopened eleven saved Joern graphs without import or default passes and retained every stored node, property and edge occurrence. The [production tests](../../cpg-rs/joern-parity/tests/function_identities.rs) gate the complete canonical references and 81 separately observed Binding, Modifier and MethodRef metadata occurrences.

Definitions receive duplicate suffixes in filename/line/column order. Literal STATIC duplicates change matching calls outside the first definition's file. The pinned inherited-static, inline-static and macro-local controls retain Joern's distinct behavior. Function references keep their original names and semantic targets while their source lines belong to their originating definitions. The full snapshot comparison found seven introduced reference-line losses in an earlier candidate and one preexisting same-file error; the final repair closes all eight. [The metadata review](astra-twelfth-evidence/metadata-review.json) retains this distinction.

All **55 commands** in the integrated capture pass. Every complete Rust snapshot is byte-identical before saving and after reopening, and all 81 selected metadata occurrences match the live reference. The new **CPG2 version 2** payload retains bindings and modifier properties; eight actual graphs from the older writer verify version 1 compatibility. Legacy CPG1 remains readable. Saving the reserved `u32::MAX` line value now fails before replacing an existing file. [Source/storage review](astra-twelfth-evidence/source-package-review.json) and [complete capture review](astra-twelfth-evidence/capture-review.json) bind these checks. This is the project's own format; Joern binary interoperability remains unsupported.

These successes have different scopes. The full Joern snapshots contain **991 nodes /4,232 edge occurrences**, compared with Rust's **985 /3,392**. Three IMPORT nodes and three DEPENDENCY nodes are missing. Dominance edges, other AST/RD relationships, property presence, coordinates and edge payloads also remain incomplete. Equal canonical projections do not establish full stored-graph equality. The [supplemental gap report](astra-twelfth-supplemental-gaps.md) retains the counts, concrete include occurrences and proposed next increment.

The named prior continuity corpus contains **248 family entries /245 unique source instances**, with three duplicate entries recorded explicitly. All **244 successful pairs** preserve their complete stdout bytes; **220 remain exact** against Joern. Matching gains and losses are both zero. The remaining `duplicate_clinit_tag` diagnostic retains empty stdout and **−6/−6** aborts. Both stderr transcripts remain; they are not claimed byte-identical. These are the enumerated continuity families, rather than a claim to have replayed every historical diagnostic. The [acceptance record](astra-twelfth-batch-acceptance.json) lists the families and actual run.

Whole-project comparisons use the unchanged **26 zlib /61 Lua files** and explicitly reuse the sixth batch's saved live Joern output. No new whole-project Joern execution is claimed. Complete matching multisets gain **27,686 zlib /2,425 Lua records**, with **no formerly matching records lost**. The 38 changed AST view occurrences affect identity fields only; paired CODE, structure and other properties stay unchanged. These record gains largely reflect corrected identity addresses and links, not that many distinct defects. [The independent source-occurrence review](astra-twelfth-evidence/whole-review.json) retains all raw additions/removals and physical ownership checks; [complete counters](astra-twelfth-batch-metrics.json) keep every canonical section and method classification.

| Exact whole METHOD ASTs, including stubs | Before | After |
|---|---:|---:|
| Zlib |160 of 410|160 of 410|
| Lua |1,438 of 2,274|1,442 of 2,274|

Lua gains `main`, `str_dump`, `writer` and `writer<duplicate>0`; no previously exact method is lost. All **61 tracked facts, 19 selected trees/stubs and three actual zlib snprintf calls** remain. Both `fixedtables` identities, scaffolds and implicit reference targets now match their physical live occurrences. Their 2,218 body rows per method remain unchanged; each full method still has six CODE differences. The missing `inflate` body remains a separate gap. Neither complete project projection is exact. [The same whole review](astra-twelfth-evidence/whole-review.json) binds these retained differences.

Final validation passes **482 workspace tests /80 result groups**, **308/308** committed and fresh live Joern comparison blocks, formatting, strict Clippy, locked release/recheck, 17 checker tests and the 73-dependency audit. Native macOS ARM64 release/archive and official real-project acceptance pass. All **149 Rust/Cargo inputs and 11,642 fixture files** remain bound to the final build; fixture Rust files are included in the source inventory, not additional compilation units. Eight ignored legacy CPG assets are explicitly included in this checkpoint, along with one earlier SARIF artifact already named and hashed by its committed measurement receipt. [Final gates](astra-twelfth-evidence/final-gates.json) retain the actual checks. No other-platform or container execution is claimed.

| Repeated build measurement | Zlib | Lua |
|---|---:|---:|
| Seconds, first /second |3.11 /3.12|10.63 /8.30|
| Peak RSS MiB, first /second |410.05 /348.14|405.64 /461.59|
| Stored nodes |70,390|120,226|

Both repetitions pass the unchanged **20-second and 512/1,024 MiB** build ceilings and produce identical graph, edge, export and zero-finding SARIF bytes. Clean/update equivalence passes for **26/61 files** in **11.23/18.41 seconds**, with separate update RSS **549.13/723.22 MiB**. Update RSS and the official helper's cumulative child RSS are different measurements from per-build peak RSS. Unrelated host activity was not controlled. Exactly **eight reviewed expectations** change: both node counts and graph/edge/export hashes. Input, archive, license, exclusion, budget and SARIF contracts stay fixed. [Resource review](astra-twelfth-evidence/resource-review.json), [measurements](astra-twelfth-evidence/resource-measurements.json) and [applied manifest](astra-twelfth-evidence/manifest-applied.json) bind these results.

Earlier workspace failures, the line-sentinel failure, the reference-location regression, and final formatting/lint failures remain recorded. The two final source corrections affect formatting and a test lookup only; both release executables remain byte-identical to those used for the eleven-case and whole-project captures. [The continuity reviews](astra-twelfth-evidence/lint-continuity-review.json) explicitly justify reusing those outputs without claiming repeat executions. The original checkout and its **13 preexisting dirty files** remain unchanged. This is a local checkpoint on `codex/astra-parity-sprint`; no push or merge is claimed.

Next: represent the observed per-include IMPORT/DEPENDENCY pairs and their namespace ownership, then separately observe inactive, repeated and unresolved include policies. Broader preprocessing/types, complete graph properties and relationships, dataflow, Joern binary transport, Scala queries/console/plugins and non-C parity remain open. These bounded counts do not estimate overall port completion. [Compatibility scope](../../cpg-rs/COMPATIBILITY.md) records those limits.
